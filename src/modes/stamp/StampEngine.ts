/**
 * StampEngine — stamp placement along pointer path; zig-zag generator.
 * TODO: implement in next iteration.
 */

export function computeStampPositions(): never[] {
  // Placeholder — returns empty array until algorithm is implemented
  return [];
}
