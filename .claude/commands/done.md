# /done — End-of-session wrap-up

1. Update `docs/CHANGELOG.md` with what was done this session
2. Update `/memory/MEMORY.md` "Project Status" line
3. Show `git diff --stat HEAD` and `git status --short`
