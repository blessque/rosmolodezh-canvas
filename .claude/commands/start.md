# /start — Session orientation

1. Read `/memory/MEMORY.md` and `/memory/NOTES.md`
2. Show `git log --oneline -5` and `git status --short`
3. Output a session brief:
   - Current milestone / iteration
   - Open tasks or known issues
   - Last known state of the project
