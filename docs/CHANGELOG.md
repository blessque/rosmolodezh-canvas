# Changelog

| Iteration | Date | Summary |
|-----------|------|---------|
| 0 | 2026-05-07 | Bootstrap: project scaffold, two-mode shell, stores, stub modes, export stubs, .claude/ setup |
| 1–3 | 2026-05-07/08 | Generator mode: GeneratorEngine (Y/X-cascade, adaptive sizing, shared baseMag, flat/angled topStyle), GalleryView (500-shape rating gallery), GeneratorPanel UI, sceneStore/uiStore wiring |
| 4 | 2026-05-08 | Vector inner-corner rounding: replaced gooey pixel-threshold renderer with Paper.js→Path2D pipeline; added `roundConcaveJunctions` (bezier fillet at concave unite() vertices); fixed asymmetric fillet radii (pullback clamp changed from `min(r*1.52, inLen/2, outLen/2)` to `min(r*1.52, (inLen+outLen)/2)`). Remaining visual bugs to fix next session. |
| 5 | 2026-05-10 | Light theme + ColorSlot: full UI flip dark→light (`#F0F2F7` bg, white cards, `#0e0f11` primary). New `ColorSlot` component — native color picker + hex input + 5-slot history swatches; shape slot pre-seeded with `#FE443B`. Default colors: `shapeColor #99ECFF`, `canvasColor #FFFFFF`. Gallery thumbnails render in live `shapeColor` on white bg via `useUIStore.getState()`. Canvas preset defaults to 4:5 (1080×1350); added A3 portrait (1240×1754) and A3 landscape/album (1754×1240); 1:1 moved to end of list. |
| 6 | 2026-05-10 | Generator v4.4–v4.5: modes pruned to lean-right/lean-left only (removed both-deep, right-anchor); cornerRadius 0.015, baseMag 0.20–0.45; flat topStyle guarantees non-zero bro; stagger ranges raised (Y-cascade 0.08–0.20, X-cascade 0.08–0.18); A3 L now correctly classified as wide (threshold > 1.0); defaultRectCount: square 75% 2-rect / 25% 3-rect, portrait-4-5 25% 2-rect / 75% 3-rect, rest always 3; X-cascade Y-stagger now independently random per step (adds ↓↑ valley and ↑↓ peak silhouettes, fixing monotone-top-line issue on wide canvas); GalleryView sweep reduced to 64 systematic shapes; ALS Hauss Next / Gorizont / HaussMono fonts added. |
| 9 | 2026-05-12 | Deploy fix: removed 4 unused variables (`dx`, `dy`, `imageTransform` in GeneratorCanvas.tsx; `stampStep` in StampCanvas.tsx) that caused `tsc -b` to fail on Render CI. Configured Render static site: build command `npm run build`, publish dir `dist`. |
| 8 | 2026-05-12 | Image masking in Generator mode: (1) `ImageTransform` type + `maskedRectIndices[]` added to `CompoundShape`; (2) new `imageMaskRenderer.ts` — clips image to mask rect(s), cover-fit, interactive handles for move/scale/corner-drag/rotate, 50% alpha outside clip in edit mode; (3) new `rectHitTest.ts` utility — point-in-rotated-quad + rect area; (4) `GeneratorCanvas` made interactive — pick mode (hover highlight + click to assign rect), edit mode (drag handles), idle mode (click mask to re-enter edit); (5) `GeneratorPanel` image upload section — drag-drop, thumbnail, Загрузить/Заменить/Удалить/Выбрать фигуру buttons; HEIC auto-converted via `heic2any` (lazy-loaded, separate 1.35 MB chunk); (6) Regenerate: image persists, largest rect auto-selected as new mask; (7) `exportPNG`/`exportSVG` render image layer (SVG uses clipPath + `<image>`). | removed 4 unused variables (`dx`, `dy`, `imageTransform` in GeneratorCanvas.tsx; `stampStep` in StampCanvas.tsx) that caused `tsc -b` to fail on Render CI. Configured Render static site: build command `npm run build`, publish dir `dist`. |
| 7 | 2026-05-10 | Stamp mode fixes (not pushed): (1) `computeCasualZigZagPath` rewritten as bouncing-ball trace — sweep axis probabilistic (wide 65% X, portrait 65% Y, crossover allowed); start position freely random on entry edge (no border-bias); slope independently random per segment 1.5–4.5 → non-physics angles, V-turns vary 25°–67°, never 90°. (2) `StampCanvas.tsx`: live preview uses `angle: 0` when image loaded (upright immediately while drawing). (3) `stampRenderer.ts`: PNG image fit changed from stretch to aspect-ratio-preserving `contain` (no more distortion on non-square images). |
